{#
/**
 * @file
 * default theme for bloc followed me
 *
 * Use to override for block.html.twig
 */
#}
<div{{ attributes }}>
    {{ title_prefix }}
    {% if label %}
        <h2{{ title_attributes }}>{{ label }} prout</h2>
    {% endif %}
    {{ title_suffix }}

    <div{{ content_attributes }}>
        {% block content %}
                {{ content }}
        {% endblock %}
    </div>
</div>